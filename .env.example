# Resend (이메일 발송)
# https://resend.com/api-keys
RESEND_API_KEY=re_xxxxx

# 발신 이메일 (선택). 없으면 Resend 기본 도메인 사용
# RESEND_FROM_EMAIL=reminder@yourdomain.com

# Cron 호출 시 인증 (선택). 설정 시 Cron 요청에 Bearer 토큰 필요
# CRON_SECRET=your-secret
